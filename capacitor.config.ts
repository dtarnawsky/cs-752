import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.v752.framework.error',
  appName: 'cs-752-framework-error',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
